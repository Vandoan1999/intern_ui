  // //  form select option
  // let element , element_option, length_from_group , length_select , length_option ,div , icon , inner_text_option
  // /*
  // element =  .form-group-select

  // l = length of .form-group-select
  // ll = length of select 
  // a = div.select-box // cái bao ngoài cùng select
  // */
  // // element = document.getElementsByClassName('form-group-select')

  // // length_from_group = element.length
  // // for(let i = 0 ; i <  length_from_group; i++){
  // //     length_select = element[i].getElementsByTagName('select').length
  // //   a = document.createElement('div')
  // //   a.setAttribute('class','select-box')
  // //   element[i].getElementsByTagName('select')
  // //   element_option = element[i].getElementsByTagName('option')
  // //   a.innerText = element_option[0].innerText
  // //   length_option = element_option.length
  // //  icon = document.createElement('i')
  // //  icon.setAttribute('class', 'fas fa-sort-down select-box_icon')
  // //  a.appendChild(icon)
   
  // //   for(let j = 0 ; j < length_select ; j ++){
  // //     element[i].appendChild(a)
  // //   }


  // //   for(let k = 1 ; k < length_option; k++ ){
  // //       inner_text_option =  element_option[k].innerText
  // //       document.createElement('ul')
  // //   }
    
  // // }